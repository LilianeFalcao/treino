export type TypeUser = {
    id: string,
    name: string,
    email: string
}

export type ProductsType = {
    id: string,
    name: string,
    price: number,
    desc: string,
    imageUrl: string
}